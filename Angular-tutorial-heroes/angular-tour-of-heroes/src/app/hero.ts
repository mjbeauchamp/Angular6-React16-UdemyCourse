export class Hero {
    //Note that all we're really doing here is creating a "type"
    //We're saying that a Hero object should have an id that's a number and a name that's a string.
    id : number;
    name : string;
}